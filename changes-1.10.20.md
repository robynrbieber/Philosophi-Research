# Changes for Version 1.10.20

## Summary

Bumped StoryLine from `1.10.19` to `1.10.20` and rebuilt the plugin bundle so the distributable files match the new version metadata.

## Files Changed

- `manifest.json`
  - Updated the Obsidian plugin version to `1.10.20`.

- `package.json`
  - Updated the npm package version to `1.10.20`.

- `package-lock.json`
  - Updated the lockfile root package version entries to `1.10.20`.

- `main.js`
  - Regenerated by `npm run build` after the version bump.

- `CHANGELOG.md`
  - Added a `1.10.20` section and moved the #148/#151 release notes there.

- `changes-1.10.20.md`
  - Added and updated this summary of changed files and work performed.

## Verification

- `npm run build` completed successfully with `obsidian-storyline@1.10.20`.
- Final diagnostics reported no errors.
